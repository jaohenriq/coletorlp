- Anotações de estruturação do código:

#include <stdio.h>
#include <stdlib.h>
#include <coletor.h>

nó *createNó(void*info) {
    Nó *novonó; //inicio da area
    novonó = malloc(1, sizeof(struct nó));
    
    novonó->informação = info;
    return novonó;
    
    void deleteNó(Nó *oldnó){
        free(oldnó);
    }
    
}

----------------------------------

typedef struct nó {
    
    struct nó* next;
} Nó;
